version: "3.7"
name: "beny-frontend"

services:
  beny-frontend:
    image: node:22
    container_name: beny-frontend
    working_dir: /usr/src/app
    volumes:
      - ../../:/usr/src/app
    ports:
      - "3000:3000"
    command: ["sh", "-c", "npm install && npm run dev"]
    networks:
      - beny

networks:
  beny:
    external: true
